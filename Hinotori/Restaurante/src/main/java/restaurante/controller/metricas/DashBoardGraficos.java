package restaurante.controller.metricas;

import java.net.URL;
import java.util.ResourceBundle;

import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.layout.StackPane;

public class DashBoardGraficos implements Initializable {

	@FXML
	private StackPane apRoot;

	@Override
	public void initialize(URL arg0, ResourceBundle arg1) {

		//JFXScrollPane.smoothScrolling((ScrollPane) background.getChildren().get(0));
		
		//background.getTopBar().getChildren().add(button);
		//background.getBottomBar().getChildren().add(title);
		
		
	}
}
